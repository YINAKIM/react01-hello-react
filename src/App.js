import { Component} from 'react';
import IterationSample from "./IterationSample";

class App extends Component{
  render() {
    // 데이터배열을 컴포넌트 배열로 변환하기
    return (
        <IterationSample/>
    );
  }
};

export default App;
